package emulator.commands;

import net.wimpi.telnetd.io.BasicTerminalIO;

import org.naturalcli.ExecutionException;
import org.naturalcli.ICommandExecutor;
import org.naturalcli.ParseResult;

public class ExamineCommandExecutor implements ICommandExecutor {

	@Override
	public void execute(ParseResult parseResult, BasicTerminalIO io)
			throws ExecutionException {

		try {
			
			// Possibilities...
			
			// if it's STATE, then dump the full state (all devices, all registers)
			
			
			// If its a device:unit, then use that.
			
			
			
			
			
		} catch (Exception e) {
		}
	}

}
